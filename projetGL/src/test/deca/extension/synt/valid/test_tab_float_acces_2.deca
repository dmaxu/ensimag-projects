{
    float elem;
    int index;
    float[][] matrix;
    matrix = [[1.1, 1.2, 1.3], [2.1, 2.2, 2.3], [3.1, 3.2, 3.3]];

    // Accès complexe

    elem = matrix[1 + 1][0]; // Évalue d'abord l'expression, puis accède 
    print("Complex access at (1+1, 0): " + elem);

    // Accès avec des variables

    index = 2;
    elem = matrix[index][index - 1]; // (2, 1) Doit retourner 3.2
    print("Variable index access: " + elem);
}